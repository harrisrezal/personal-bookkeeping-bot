{
  "name": "personal-bookkeeping-bot",
  "version": "1.0.0",
  "description": "Telegram bookkeeping bot backed by Google Sheets. Deployable on Vercel, Railway, Render, or any Node.js host.",
  "type": "commonjs",
  "scripts": {
    "start": "node server.js",
    "test": "node test/bot.test.js",
    "set-webhook": "node scripts/set-webhook.js",
    "delete-webhook": "node scripts/delete-webhook.js",
    "get-webhook-info": "node scripts/get-webhook-info.js"
  },
  "engines": {
    "node": ">=20"
  },
  "dependencies": {
    "express": "^4.18.0"
  }
}
